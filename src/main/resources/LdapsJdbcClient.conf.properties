# Copy this file outside the repository and replace every example value.
connection.url = jdbc:hive2://impala.example.com:21050/default;ssl=true
jdbc.driver.class.name = org.apache.hive.jdbc.HiveDriver
connection.username = example-user
connection.password = replace-me
connection.truststore.jks.file = /absolute/path/to/your/truststore.jks
connection.truststore.jks.password = replace-me
connection.query = SELECT COUNT(*) FROM example_table
