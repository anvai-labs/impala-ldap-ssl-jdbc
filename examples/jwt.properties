# Short-lived JWT example. Export IMPALA_JWT; never place JWTString in this file or URL.
connection.auth.mode = JWT
connection.url = jdbc:impala://impala.example.com:28000/default;AuthMech=14;SSL=1;TransportMode=http;httpPath=cliservice
jdbc.driver.class.name = com.cloudera.impala.jdbc41.Driver
connection.query = SELECT CURRENT_USER()
